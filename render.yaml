services:
  - type: web
    name: lafsh-security-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run python/dashboard.py --server.port $PORT --server.address 0.0.0.0
